<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.dao.NotificationMapper">
    
    <resultMap id="BaseResultMap" type="com.example.springboot.entity.Notification">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="title" column="title" />
        <result property="content" column="content" />
        <result property="type" column="type" />
        <result property="relatedId" column="related_id" />
        <result property="isRead" column="is_read" />
        <result property="createTime" column="create_time" />
    </resultMap>

    <insert id="insert" parameterType="com.example.springboot.entity.Notification">
        INSERT INTO notification (user_id, title, content, type, related_id, is_read)
        VALUES (#{userId}, #{title}, #{content}, #{type}, #{relatedId}, 0)
    </insert>

    <insert id="batchInsert">
        INSERT INTO notification (user_id, title, content, type, related_id, is_read, create_time) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.title}, #{item.content}, #{item.type}, #{item.relatedId}, 0, #{item.createTime})
        </foreach>
    </insert>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM notification WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="countUnreadByUserId" resultType="int">
        SELECT COUNT(*) FROM notification WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markAsRead">
        UPDATE notification SET is_read = 1 WHERE id = #{id}
    </update>

    <update id="markAllAsReadByUserId">
        UPDATE notification SET is_read = 1 WHERE user_id = #{userId}
    </update>

    <delete id="delete">
        DELETE FROM notification WHERE id = #{id}
    </delete>
</mapper>
