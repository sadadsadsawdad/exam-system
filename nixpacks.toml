[phases.setup]
nixPkgs = ["maven", "jdk17"]

[phases.build]
cmds = ["cd springboot && mvn clean package -DskipTests"]

[start]
cmd = "java -Dserver.port=$PORT -jar springboot/target/springboot-0.0.1-SNAPSHOT.jar"
